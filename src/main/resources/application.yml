server:
  servlet:
    context-path: /api/v1
  port: 8080

spring:
  datasource:
    url: ${DB_URL}
    driver-class-name: ${DB_PLATFORM}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    hikari:
      minimumIdle: 4
      maximumPoolSize: 8
      idleTimeout: 15000
      poolName: SpringBootJPAHikariCP
      maxLifetime: 1800000
      connectionTimeout: 30000

  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: false
  sql:
    init:
      encoding: UTF-8


  jackson:
    serialization:
      write-dates-as-timestamps: false